/**
 * Shim for `import { Tooltip } from "@coinbase/cds-web/overlays/Tooltip"`.
 * The published package exposes `overlays/tooltip/Tooltip` (lowercase `tooltip` dir).
 */
export { Tooltip } from "@coinbase/cds-web/overlays/tooltip/Tooltip"
